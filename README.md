# hsearch

Hactar's Hyperpuzzle Search Program

## To-do

- [ ] remove `notation.rs`
- [ ] `flate2` compression on pruning tables
- [ ] consider alternate strategy: never explicitly solve `P`. just build `I` and `O` (like Roux)
